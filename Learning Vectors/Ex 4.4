import java.util.Scanner;

public class ex 4 {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        int qtN, posicion = 0;
        double bigger = -99;

        System.out.println("Insert how many numbers will you enter: ");
        qtN = sc.nextInt();

        double totN[] = new double[qtN];

        for (int i = 0; i < totN.length; i++) {
            System.out.printf("Insert the %dº number: ", (i + 1));
            totN[i] = sc.nextDouble();

            if (totN[i] > bigger) {
                bigger = totN[i];
                posicion = i;
            }
        }
        System.out.printf("\Biggest valor = %.2f\n", bigger);
        System.out.println("Posicion of he biggest valor = " + posicion);

        sc.close();
    }
}
